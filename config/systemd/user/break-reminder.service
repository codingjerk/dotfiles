[Unit]
Description=Send an activity-aware break reminder
After=graphical-session.target
Requisite=graphical-session.target

[Service]
Type=oneshot
# VAR: fg_half_dim
Environment="FG_HALF_DIM=#{fg_half_dim}"
# VAR: repo
ExecStart=/usr/bin/sh "{repo}/scripts/break-reminder.sh"
