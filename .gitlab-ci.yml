install:
  image: archlinux/base

  before_script:
    - pacman -Sy --noconfirm git zsh sudo
    - touch settings.sh

  script:
    - sh tools/one-command-install.sh
    - "[ ! -f ~/.zshenv ]"
    - "[ -f ~/.pam_environment ]"
